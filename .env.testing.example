# This file is a template for setting up your testing environment.
# Copy this file to .env.testing and replace the placeholder values
# with your actual test credentials. DO NOT commit .env.testing to git.

# Supabase testing credentials
# You should use a separate Supabase project for testing to avoid data loss.
NEXT_PUBLIC_SUPABASE_URL="your_test_supabase_url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_test_supabase_anon_key"

# The service role key is required for test cleanup operations (e.g., deleting test users).
# Find it in your Supabase project settings under API > Project API keys.
SUPABASE_SERVICE_ROLE_KEY="your_test_supabase_service_role_key"

# Resend API key
# For our real tests, we will mock the email sending to snapshot the content.
# You can use a dummy key like "re_test..." or a real one.
RESEND_API_KEY="re_test_..."